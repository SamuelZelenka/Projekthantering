using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cardscript2 : MonoBehaviour
{
    
    // Update is called once per frame
    void Update()
    {/*
        RaycastHit hit;

        if (Physics.Raycast(transform.position, Vector3.up, out hit, Mathf.Infinity))
        {
            if (hit.collider.transform.position.z < this.gameObject.transform.position.z)
            {
                gameObject.transform.position = new Vector3(0.16f, 2.7f, 4.08f + 1.50f);
            }

            else if (hit.collider.transform.position.z > this.gameObject.transform.position.z)
            {
                gameObject.transform.position = new Vector3(0.16f, 2.7f, 4.08f - 1.50f);
            }

        }*/
    }
}
